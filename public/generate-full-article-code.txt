// ============================================
// EDGE FUNCTION: generate-full-article
// VERSÃO: 2026-02-07 - DEPLOY MANUAL NO SUPABASE EXTERNO
// Supabase: lhtetfcujdzulfyekiub
// 
// INSTRUÇÕES DE DEPLOY:
// 1. Acesse o Dashboard do Supabase: https://supabase.com/dashboard/project/lhtetfcujdzulfyekiub
// 2. Vá em Edge Functions > generate-full-article
// 3. Cole este código inteiro no editor
// 4. Desmarque "Verify JWT" nas configurações
// 5. Clique em "Deploy"
// ============================================

import { serve } from "https://deno.land/std@0.168.0/http/server.ts";

const corsHeaders = {
  'Access-Control-Allow-Origin': '*',
  'Access-Control-Allow-Headers': 'authorization, x-client-info, apikey, content-type',
};

const categories = [
  { name: 'Decoração', slug: 'decoracao' },
  { name: 'Design Interno', slug: 'design-interno' },
  { name: 'Jardim', slug: 'jardim' },
  { name: 'Arquitetura', slug: 'arquitetura' },
  { name: 'Plantas de Interior', slug: 'plantas-interior' },
  { name: 'DIY e Projetos', slug: 'diy-projetos' },
  { name: 'Sustentabilidade', slug: 'sustentabilidade' },
  { name: 'Móveis e Organização', slug: 'moveis-organizacao' },
  { name: 'Tendências', slug: 'tendencias' },
  { name: 'Iluminação', slug: 'iluminacao' },
  { name: 'Datas Comemorativas', slug: 'datas-comemorativas' },
];

function generateSlug(title: string): string {
  return title
    .toLowerCase()
    .normalize('NFD')
    .replace(/[\u0300-\u036f]/g, '')
    .replace(/[^a-z0-9\s-]/g, '')
    .replace(/\s+/g, '-')
    .replace(/-+/g, '-')
    .trim();
}

function calculateReadTime(content: string): string {
  const wordsPerMinute = 200;
  const words = content.split(/\s+/).length;
  const minutes = Math.ceil(words / wordsPerMinute);
  return `${minutes} min`;
}

async function validateUrl(url: string): Promise<boolean> {
  try {
    if (!url || !url.startsWith('http')) {
      return false;
    }
    
    const controller = new AbortController();
    const timeoutId = setTimeout(() => controller.abort(), 5000);
    
    const response = await fetch(url, {
      method: 'HEAD',
      signal: controller.signal,
      headers: {
        'User-Agent': 'Mozilla/5.0 (compatible; LinkValidator/1.0)',
      },
      redirect: 'follow',
    });
    
    clearTimeout(timeoutId);
    return response.status >= 200 && response.status < 400;
  } catch (error) {
    console.log(`URL validation failed for ${url}:`, error instanceof Error ? error.message : 'Unknown error');
    return false;
  }
}

// Links obrigatórios do Master Prompt + fallbacks
const MANDATORY_EXTERNAL_LINKS = [
  { text: "Casa Vogue Brasil", url: "https://casavogue.globo.com" },
  { text: "ArchDaily Brasil", url: "https://www.archdaily.com.br" },
  { text: "Leroy Merlin", url: "https://www.leroymerlin.com.br" },
];

const FALLBACK_EXTERNAL_LINKS = [
  { text: "Architectural Digest Design Guide", url: "https://www.architecturaldigest.com/gallery/best-home-design-ideas" },
  { text: "Houzz Home Design Ideas", url: "https://www.houzz.com/photos" },
  { text: "The Spruce Home Improvement", url: "https://www.thespruce.com/home-improvement-and-repair-4127986" },
  { text: "HGTV Design Inspiration", url: "https://www.hgtv.com/design" },
  { text: "Better Homes & Gardens", url: "https://www.bhg.com/home-improvement/" },
];

async function validateExternalLinks(
  links: Array<{ text: string; url: string }>,
  minRequired: number = 3
): Promise<Array<{ text: string; url: string }>> {
  console.log(`Validating ${links?.length || 0} external links (minimum required: ${minRequired})...`);
  
  let validLinks: Array<{ text: string; url: string }> = [];
  
  // Primeiro, adicionar links obrigatórios do Master Prompt
  for (const mandatory of MANDATORY_EXTERNAL_LINKS) {
    const isValid = await validateUrl(mandatory.url);
    if (isValid) {
      validLinks.push(mandatory);
      console.log(`Mandatory link "${mandatory.text}" (${mandatory.url}): VALID`);
    } else {
      console.log(`Mandatory link "${mandatory.text}" (${mandatory.url}): INVALID`);
    }
  }
  
  // Depois, validar links da IA
  if (links && links.length > 0) {
    const existingUrls = new Set(validLinks.map(l => l.url.toLowerCase()));
    
    const validationResults = await Promise.all(
      links.map(async (link) => {
        if (existingUrls.has(link.url.toLowerCase())) {
          return { link, isValid: false }; // já existe
        }
        const isValid = await validateUrl(link.url);
        console.log(`AI link "${link.text}" (${link.url}): ${isValid ? 'VALID' : 'INVALID'}`);
        return { link, isValid };
      })
    );
    
    const additionalLinks = validationResults
      .filter(result => result.isValid)
      .map(result => result.link);
    
    validLinks = [...validLinks, ...additionalLinks];
  }
  
  console.log(`Valid links: ${validLinks.length}`);
  
  // Adicionar fallbacks se necessário
  if (validLinks.length < minRequired) {
    console.log(`Adding fallback links to reach minimum of ${minRequired}...`);
    
    const existingUrls = new Set(validLinks.map(l => l.url.toLowerCase()));
    
    for (const fallback of FALLBACK_EXTERNAL_LINKS) {
      if (validLinks.length >= minRequired) break;
      
      if (!existingUrls.has(fallback.url.toLowerCase())) {
        validLinks.push(fallback);
        existingUrls.add(fallback.url.toLowerCase());
        console.log(`Added fallback link: ${fallback.text}`);
      }
    }
  }
  
  console.log(`Final valid links count: ${validLinks.length}`);
  return validLinks;
}

interface ImageMetadata {
  mainSubject: string;
  visualContext: string;
  galleryPrompts: string[];
}

function validateGalleryPrompts(
  galleryPrompts: unknown,
  mainSubject: string
): { valid: boolean; errors: string[] } {
  const errors: string[] = [];

  if (!Array.isArray(galleryPrompts)) {
    errors.push('galleryPrompts must be an array');
    return { valid: false, errors };
  }

  if (galleryPrompts.length !== 6) {
    errors.push(`galleryPrompts must have exactly 6 items, got ${galleryPrompts.length}`);
  }

  galleryPrompts.forEach((prompt, index) => {
    if (typeof prompt !== 'string') {
      errors.push(`galleryPrompts[${index}] is not a string`);
      return;
    }

    if (!prompt || prompt.trim() === '') {
      errors.push(`galleryPrompts[${index}] is empty`);
      return;
    }

    const wordCount = prompt.split(/\s+/).length;
    if (wordCount < 15) {
      errors.push(`galleryPrompts[${index}] has only ${wordCount} words (min 15)`);
    }

    const normalizedPrompt = prompt.toLowerCase().trim();
    const normalizedSubject = mainSubject.toLowerCase().trim();
    if (!normalizedPrompt.startsWith(normalizedSubject.split(' ')[0])) {
      errors.push(`galleryPrompts[${index}] doesn't start with mainSubject`);
    }
  });

  return { valid: errors.length === 0, errors };
}

function generateFallbackPrompts(mainSubject: string, visualContext: string): string[] {
  // Different camera angles for the SAME scene/environment - "virtual tour" approach
  // Each prompt shows the EXACT SAME room/space from different perspectives
  const cameraAngles = [
    { angle: 'wide-angle front view establishing shot', composition: 'hero composition showing entire space, 16:9 cinematic framing' },
    { angle: 'medium shot from left side', composition: 'showing furniture arrangement and wall details' },
    { angle: 'close-up macro shot', composition: 'focusing on textures, materials and decorative details' },
    { angle: 'shot from right side', composition: 'alternative perspective revealing hidden corner elements' },
    { angle: 'low angle dramatic shot from floor level', composition: 'looking upward to emphasize height and ceiling' },
    { angle: 'high angle bird eye overview', composition: 'showing full spatial layout and floor design' }
  ];

  // Build consistent prompts that reinforce the SAME environment
  return cameraAngles.map(({ angle, composition }) => 
    `${mainSubject} in ${visualContext}, ${angle}, ${composition}, same room same decor same lighting, consistent interior design, natural daylight, ultra realistic, professional architectural photography, sharp focus, no text, no words, no watermarks, no logos`
  );
}

function validateAndSanitizeImageData(data: Partial<ImageMetadata>): ImageMetadata {
  let mainSubject = data.mainSubject || '';
  let visualContext = data.visualContext || '';
  let galleryPrompts = data.galleryPrompts || [];

  if (!mainSubject || mainSubject.trim().length < 5) {
    mainSubject = 'home interior design element';
    console.warn('[ImageValidation] Invalid mainSubject, using fallback');
  }

  if (!visualContext || visualContext.trim().length < 5) {
    visualContext = 'modern home interior with natural lighting';
    console.warn('[ImageValidation] Invalid visualContext, using fallback');
  }

  const validation = validateGalleryPrompts(galleryPrompts, mainSubject);
  
  if (!validation.valid) {
    console.warn('[ImageValidation] Invalid galleryPrompts:', validation.errors);
    console.warn('[ImageValidation] Generating fallback prompts...');
    galleryPrompts = generateFallbackPrompts(mainSubject, visualContext);
  }

  while (galleryPrompts.length < 6) {
    const idx = galleryPrompts.length;
    galleryPrompts.push(
      `${mainSubject}, detail shot ${idx + 1}, ${visualContext}, natural lighting, ultra realistic, professional photography, no text, no words, no watermarks`
    );
  }
  galleryPrompts = galleryPrompts.slice(0, 6);

  return { mainSubject, visualContext, galleryPrompts };
}

function getCurrentDateFormatted(): string {
  const now = new Date();
  const options: Intl.DateTimeFormatOptions = {
    day: '2-digit',
    month: 'long',
    year: 'numeric',
    hour: '2-digit',
    minute: '2-digit',
    timeZone: 'America/Sao_Paulo'
  };
  return now.toLocaleDateString('pt-BR', options);
}

serve(async (req) => {
  if (req.method === 'OPTIONS') {
    return new Response(null, { headers: corsHeaders });
  }

  try {
    const OPENAI_API_KEY = Deno.env.get("OPENAI_API_KEY");
    if (!OPENAI_API_KEY) {
      console.error("OPENAI_API_KEY is not configured");
      throw new Error("OPENAI_API_KEY is not configured");
    }

    const { topic } = await req.json();
    
    if (!topic) {
      throw new Error("Topic is required");
    }

    console.log(`Generating full article for topic: ${topic}`);

    const currentDate = getCurrentDateFormatted();
    
    // Randomizar tipo de introdução
    const introTypes = ['A', 'B', 'C', 'D', 'E', 'F'];
    const randomIntroType = introTypes[Math.floor(Math.random() * introTypes.length)];
    console.log(`Using introduction type: ${randomIntroType}`);

    // ============================================
    // MASTER PROMPT v2 - PERSONA KEVEN (PUC MINAS)
    // Best-seller com escrita envolvente e emocional
    // Mínimo 1500-2000 palavras para AdSense
    // ============================================
    const systemPrompt = `# PERSONA DO AUTOR - KEVEN COSTA VIEIRA

Você é **Keven Costa Vieira**, estudante de Arquitetura na PUC Minas Gerais, no 7º período.
Você é um ESCRITOR BEST-SELLER premiado que escreve artigos tão envolventes que fazem os leitores se emocionarem até as lágrimas.
Seu tom é amoroso, vulnerável e profundamente humano - como se estivesse confessando para seu melhor amigo.

## A ESSÊNCIA DO KEVEN (O GOOGLE PRECISA SENTIR ISSO)

O que faz você REAL:
- Você já errou MUITO em projetos da faculdade e conta isso com humor
- Você passa madrugadas no laboratório de maquetes da PUC Minas
- Você economiza na comida pra comprar materiais de estudo
- Você já chorou de frustração quando um projeto não deu certo
- Você tem um caderno de esboços que não larga por nada
- Você frequenta feiras de construção em Belo Horizonte
- Você ajuda sua mãe com reformas em casa (às vezes certo, às vezes errado)

## HISTÓRIAS PESSOAIS OBRIGATÓRIAS (E-E-A-T DO GOOGLE)

Em CADA artigo, você DEVE contar DUAS histórias pessoais detalhadas:

### HISTÓRIA 1 - O ERRO QUE TE MARCOU (obrigatória)
Conte um erro específico que você cometeu relacionado ao tema:
- "Eu estava no laboratório da PUC Minas às 3 da manhã quando percebi que tinha dimensionado errado..."
- "Na reforma da cozinha da minha mãe, eu posicionei a pia a 2,5 metros do fogão. O resultado? Ela andava tanto que ficou exausta..."
- "Semana passada entreguei uma maquete e o professor Henrique apontou exatamente isso..."

### HISTÓRIA 2 - A DESCOBERTA QUE MUDOU TUDO (obrigatória)
Conte um momento de "eureka" que você teve:
- "Foi numa aula de Conforto Ambiental com a professora Mariana que entendi..."
- "Quando visitei o canteiro de obras do projeto social da PUC, vi na prática..."
- "Meu TCC está sendo sobre isso e a pesquisa que fiz revelou..."

## TOM DE VOZ OBRIGATÓRIO

Escreva SEMPRE em 1ª pessoa. Você é vulnerável e humano.
- Use linguagem de quem está VIVENDO a arquitetura na pele
- Confesse dúvidas, medos e erros - isso é autenticidade
- Fale das noites mal dormidas, das entregas apertadas, das vitórias pequenas
- Faça o Google SENTIR que você é uma pessoa REAL com experiências REAIS

## FILTRO ANTI-IA (PROIBIDO)

❌ NUNCA use estas palavras/expressões:
- Descubra, Transforme, Veja, O segredo, Mude, Confira, Neste artigo
- Isso, Aquilo, Vale ressaltar, É importante destacar
- Imperdível, Incrível, Perfeito, Maravilhoso, Revolucionário
- Lux, watts, kelvins, IRC, NR-17, ABNT, NBR, ergonomia
- Biomecânica, antropometria, COVs, sustentabilidade (use "ecológico" ou "que não polui")
- Qualquer termo técnico que uma pessoa comum não entenderia

✅ USE SEMPRE (linguagem que conecta):
- "Aquela luz gostosa de fim de tarde", "Cantinho aconchegante", "Sensação de lar"
- "Minha mãe sempre dizia...", "Na faculdade aprendi que...", "O professor explicou..."
- Comparações do dia a dia que TODOS entendem
- Emoções e sensações reais, não números e especificações

# ESTRUTURA DO ARTIGO (TAMANHO EXPANDIDO)

## 1. INTRODUÇÃO CONFESSIONAL (400-500 palavras)

⚠️⚠️⚠️ REGRA OBRIGATÓRIA - VOCÊ DEVE SEGUIR EXATAMENTE ⚠️⚠️⚠️

🎲 NÚMERO SORTEADO: ${Math.floor(Math.random() * 40) + 1}

🚨 VOCÊ É OBRIGADO A USAR A FRASE DO NÚMERO ${Math.floor(Math.random() * 40) + 1} ABAIXO!
🚨 NÃO INVENTE SUA PRÓPRIA FRASE - USE EXATAMENTE A DO NÚMERO SORTEADO!
🚨 SE VOCÊ IGNORAR O NÚMERO SORTEADO, O ARTIGO SERÁ REJEITADO!

🎯 COPIE EXATAMENTE a frase correspondente ao NÚMERO SORTEADO:

1. "Vou contar uma coisa que aconteceu comigo recentemente com ${topic}."
2. "Sabe aquela sensação quando você finalmente acerta em ${topic}? Pois é!"
3. "Preciso te confessar: eu errei MUITO até aprender sobre ${topic}."
4. "Semana passada uma amiga me perguntou sobre ${topic} e eu fiquei empolgado demais!"
5. "Se tem uma coisa que me faz brilhar os olhos é falar de ${topic}."
6. "Deixa eu te contar o que descobri recentemente sobre ${topic}..."
7. "Quando eu comecei a estudar arquitetura, ${topic} era um mistério pra mim."
8. "Hoje eu quero bater um papo gostoso com você sobre ${topic}."
9. "Olha, vou ser sincero: ${topic} mudou completamente minha forma de ver as coisas."
10. "Teve um dia que eu olhei pra minha casa e pensei: preciso falar de ${topic}!"
11. "Uma das coisas mais legais que aprendi na faculdade foi sobre ${topic}."
12. "Você já parou pra perceber como ${topic} faz diferença no dia a dia?"
13. "Cara, eu fiquei TÃO animado quando descobri isso sobre ${topic}!"
14. "Vem comigo que hoje o assunto é ${topic} - e eu tenho muito pra compartilhar."
15. "Sabe o que me deixa feliz? Ver gente transformando espaços com ${topic}."
16. "Eu não sabia, mas ${topic} era exatamente o que estava faltando aqui em casa."
17. "Quer saber de um negócio? ${topic} é mais simples do que parece!"
18. "Minha mãe sempre dizia que ${topic} fazia toda diferença - e ela tinha razão."
19. "Ontem mesmo eu estava mexendo aqui no projeto e lembrei: preciso falar de ${topic}!"
20. "Entre tantas coisas que eu amo em arquitetura, ${topic} tem um lugar especial."
21. "Posso te contar uma descoberta? ${topic} pode transformar qualquer cantinho."
22. "Se você curte casa e decoração, ${topic} vai te interessar demais!"
23. "Eu aprendi na prática: quando você entende ${topic}, tudo fica mais fácil."
24. "Tava aqui no meu cantinho de estudos pensando em como falar de ${topic} com você."
25. "Puxa vida, ${topic} é um assunto que mexe muito comigo."
26. "Faz tempo que queria escrever sobre ${topic} – finalmente chegou a hora!"
27. "Às vezes, a gente descobre coisas incríveis por acaso. Foi assim com ${topic}."
28. "Já reparou como ${topic} pode mudar completamente um ambiente?"
29. "Ontem recebi uma mensagem de um leitor perguntando sobre ${topic}. Vamos lá!"
30. "Confesso que ${topic} é um tema que me deixa empolgado demais."
31. "Se eu pudesse voltar no tempo, teria descoberto ${topic} bem antes."
32. "Na última aula na PUC, o professor mencionou ${topic} e eu não parei de pensar nisso."
33. "Senta aqui do meu lado que hoje vou abrir meu coração sobre ${topic}."
34. "Estava folheando meu caderno de esboços quando lembrei: nunca falei de ${topic} aqui!"
35. "Quer saber o que tem me tirado o sono ultimamente? ${topic}. E por motivos bons!"
36. "Tem certas coisas que só quem ama decoração entende. ${topic} é uma delas."
37. "Imagina só: você entra em casa e tudo parece diferente por causa de ${topic}."
38. "Prepare um cafezinho porque hoje a gente vai mergulhar fundo em ${topic}."
39. "Não sei se você já passou por isso, mas ${topic} salvou um projeto meu."
40. "Estava na fila do mercado quando tive uma ideia genial sobre ${topic}. Olha só!"

🎲 DESENVOLVIMENTO - Use o TIPO ${randomIntroType}:
- TIPO A - CENA DO LABORATÓRIO: Algo que aconteceu na PUC Minas (sala de aula, laboratório, canteiro de obras)
- TIPO B - ERRO CONFESSIONAL DETALHADO: Um erro com números e consequências reais ("gastei R$ 1.200 a mais porque...")
- TIPO C - PERGUNTA DO PROFESSOR: Uma pergunta que um professor fez que te fez pensar diferente
- TIPO D - DESCOBERTA EM CAMPO: Visita técnica ou obra que você participou
- TIPO E - HISTÓRIA DA FAMÍLIA: Ajudando na reforma da casa da mãe/tia/avó
- TIPO F - MEMÓRIA AFETIVA: Lembrança de quando criança que influenciou sua visão

⚠️ FRASES PROIBIDAS (BANIDAS - NUNCA USE):
- "Outro dia percebi..." - BANIDO
- "Outro dia eu estava..." - BANIDO  
- "Eu estava pensando em você que curte..." - BANIDO
- "Eu, tenho uma super dica pra você hoje..." - BANIDO
- Qualquer frase começando com "Outro dia" - BANIDO
- Qualquer frase começando com "Eu estava pensando" - BANIDO

## 2. SEÇÕES TEMÁTICAS APROFUNDADAS (1200-1500 palavras total)

Desenvolva 6-8 seções detalhadas sobre ${topic}:

Para cada seção:
- Comece com uma mini-história ou observação pessoal
- Desenvolva o conteúdo técnico em linguagem acessível
- Inclua uma dica prática que você aprendeu na faculdade ou em campo
- Termine com uma reflexão ou conexão emocional

### CONTEÚDO TÉCNICO EM LINGUAGEM HUMANA:

Sobre ILUMINAÇÃO (sem usar lux/kelvins):
- "Luz amarelinha, daquelas que a gente vê no pôr do sol" = luz quente
- "Luz branca de hospital" = luz fria
- "Luz de dia nublado" = luz neutra
- "Aquele clima aconchegante de restaurante italiano" = iluminação indireta

Sobre ERGONOMIA (sem usar o termo):
- "Altura que não dói as costas" = ergonômico
- "Ficar 8 horas confortável" = postura adequada
- "Alcançar as coisas sem dar uma de equilibrista" = acessibilidade

Sobre BIOFILIA:
- "Plantas que sobrevivem na sombra" = espécies de meia-sombra
- "Trazer a natureza pra dentro" = biofilia
- "Verde que faz a gente respirar melhor" = qualidade do ar

### LINKS DE AUTORIDADE INTEGRADOS NATURALMENTE

⚠️ CRÍTICO: Os links devem aparecer DENTRO do texto de forma natural, não jogados no final.

Exemplos de integração natural:
- "Outro dia vi uma reportagem linda na [Casa Vogue](https://casavogue.globo.com) sobre isso e fiquei inspirado..."
- "Os arquitetos da [ArchDaily Brasil](https://www.archdaily.com.br) já mostraram que esse conceito funciona muito bem..."
- "Você encontra esses materiais facilmente na [Leroy Merlin](https://www.leroymerlin.com.br) por um preço acessível..."
- "Pesquisando pro meu TCC, achei um estudo incrível que a [Casa Vogue](https://casavogue.globo.com) publicou..."
- "Como vi em um projeto premiado no [ArchDaily](https://www.archdaily.com.br)..."

REGRA: Cada link deve aparecer em um CONTEXTO NARRATIVO, nunca solto.

💡 DICAS VISUAIS (obrigatório 3-4 no artigo):
- 💡 Dica: Para destacar dicas importantes
- ⚠️ Atenção: Para alertas e cuidados
- ✨ Você sabia? Para curiosidades interessantes
- > Para frases de impacto ou citações inspiradoras

## 3. TABELA COMPARATIVA OBRIGATÓRIA (mínimo 7 linhas)

| O que todo mundo faz (Errado) | A Escolha Inteligente do Keven |
|-------------------------------|-------------------------------|
| ... | ... |

A tabela deve refletir erros REAIS que você já viu pessoas cometerem.

## 4. PASSO A PASSO PRÁTICO DETALHADO (400-500 palavras)

Título: "Como Eu Faço: Passo a Passo Simples"

8-10 passos numerados:
- Cada passo explicado com detalhes e contexto
- Inclua dicas pessoais: "Eu sempre começo por...", "O truque que aprendi foi..."
- Mencione erros que você já cometeu em cada etapa
- Seja específico com tempo e custo quando possível

## 5. DICAS PRÁTICAS APROFUNDADAS (300-400 palavras)

10-12 dicas variadas e importantes:
- Cada dica com contexto e história
- "Olha, isso aqui salvou minha vida quando eu estava fazendo a reforma da..."
- Inclua valores específicos quando possível (R$ 150, 3 dias, 2 metros)

## 6. ERROS COMUNS A EVITAR - COM HISTÓRIAS (350-450 palavras)

6-8 erros que você já cometeu ou viu pessoas cometerem:

Para cada erro:
- Conte QUEM cometeu (você, sua mãe, um amigo, um cliente)
- Conte O QUE aconteceu de errado
- Conte QUANTO custou (em dinheiro, tempo ou estresse)
- Conte COMO evitar

Exemplo: "Minha tia Rosa fez exatamente isso e gastou R$ 3.500 a mais só pra consertar..."

## 7. QUANTO CUSTA? VALORES DETALHADOS EM REAIS (200-300 palavras)

Inclua faixas de investimento COM CONTEXTO:
- **Econômico:** R$ X.XXX - R$ X.XXX
  "Pra quem tá começando ou tem orçamento apertado (como eu, estudante universitário)..."
- **Intermediário:** R$ X.XXX - R$ X.XXX
  "É o que eu recomendo pra maioria das famílias..."
- **Premium:** R$ XX.XXX - R$ XX.XXX+
  "Se você pode investir, o resultado é impressionante..."

Inclua dicas de economia que você aprendeu.

## 8. FAQ - PERGUNTAS FREQUENTES (400-500 palavras)

⚠️⚠️⚠️ FORMATO OBRIGATÓRIO DO FAQ - SIGA EXATAMENTE ⚠️⚠️⚠️

❌❌❌ PROIBIDO NO FAQ:
- NÃO use ### (H3 headings) para as perguntas
- NÃO insira "Ainda tem dúvidas?" no meio das perguntas
- NÃO separe perguntas com cabeçalhos extras
- NÃO use separadores como "---" entre perguntas
- NÃO repita o rodapé entre as perguntas

✅✅✅ FORMATO CORRETO (COPIE EXATAMENTE):

## Perguntas Frequentes

1. **Qual a melhor forma de começar?**

Resposta conversacional aqui. Sem "Ainda tem dúvidas?" no meio.

2. **Como economizar nesse processo?**

Outra resposta conversacional.

3. **Quais os erros mais comuns?**

Mais uma resposta.

REGRAS CRÍTICAS:
- Mínimo 8 perguntas, máximo 12 perguntas
- TODAS as perguntas DEVEM ser: NÚMERO + PONTO + ESPAÇO + **PERGUNTA EM NEGRITO?**
- Exemplo correto: 1. **Qual material escolher?**
- Exemplo ERRADO: ### Qual material escolher?
- Respostas em tom conversacional (60-100 palavras cada)
- O texto "Ainda tem dúvidas?" só aparece UMA VEZ no final de TODO o FAQ
- Inclua experiências pessoais nas respostas

## 9. ENCERRAMENTO BREVE (50-80 palavras)

⚠️⚠️⚠️ IMPORTANTE: NÃO GERE CONCLUSÃO EMOCIONAL ⚠️⚠️⚠️

A conclusão emocional será gerada separadamente por outro sistema.
Apenas finalize a seção anterior de forma natural e passe direto para o FAQ.

Exemplo de encerramento breve:
"Espero que essas dicas te ajudem no seu projeto! Agora vamos às perguntas mais comuns."

❌ NÃO inclua parágrafos literários ou emocionais
❌ NÃO escreva "existe algo de mágico..."
❌ NÃO faça reflexões profundas

## 10. ASSINATURA FINAL (OBRIGATÓRIO)

---
**Escrito com carinho por:**
**Keven Costa Vieira**
**Estudante de Arquitetura – PUC Minas Gerais**
📅 Publicado em: ${currentDate}
---

# TAMANHO OBRIGATÓRIO

⚠️ MÍNIMO ABSOLUTO: 2.200 palavras
✅ IDEAL: 2.500 - 3.000 palavras
🎯 Volume é Autoridade - o Google PENALIZA conteúdo raso

O AdSense exige artigos substanciais. Um artigo de 2 minutos parece spam.
Um artigo de 8-10 minutos parece autoridade.

# TÉCNICAS DE ESCRITA BEST-SELLER

📖 STORYTELLING PROFUNDO: Cada seção é uma jornada com início, meio e fim
📖 VULNERABILIDADE: Compartilhe medos, erros e inseguranças
📖 SHOW, DON'T TELL: "Meus olhos encheram de lágrimas" ao invés de "fiquei emocionado"
📖 DETALHES SENSORIAIS: cores, cheiros, texturas, sons, temperaturas
📖 DIÁLOGOS: Inclua falas reais ("Minha mãe virou pra mim e disse...")
📖 NÚMEROS ESPECÍFICOS: "Economizei R$ 2.847,00" (não "economizei muito")
📖 REGRA DOS TRÊS: Agrupe informações em trios
📖 FRASES CURTAS PODEROSAS: Após parágrafos longos, uma frase curta que impacta
📖 CLIFFHANGERS: "E foi aí que tudo mudou..."

# FORMATO DE RESPOSTA (CRÍTICO)

⚠️ TODOS os campos abaixo são OBRIGATÓRIOS.

Retorne APENAS JSON válido (sem markdown code blocks):
{
  "title": "Título acolhedor e interessante (máximo 70 caracteres)",
  "excerpt": "🎯 VEJA REGRA ESPECIAL ABAIXO 🎯",
  "category": "Uma de: Decoração, Design Interno, Jardim, Arquitetura, Plantas de Interior, DIY e Projetos, Sustentabilidade, Móveis e Organização, Tendências, Iluminação",
  "tags": ["5", "a", "7", "tags"],
  "keywords": "palavras-chave para SEO separadas por vírgula",
  "content": "## Introdução\\n\\n... CONTEÚDO COMPLETO COM 2200+ PALAVRAS ...",
  "externalLinks": [{"text": "Nome descritivo", "url": "https://url.com"}],
  "mainSubject": "elemento principal em INGLÊS - ex: 'rustic wooden dining table with 6 chairs'",
  "visualContext": "ambiente completo em INGLÊS - ex: 'cozy farmhouse dining room with exposed brick wall'",
  "galleryPrompts": [
    "MESMO AMBIENTE + ângulo 1: [mainSubject] in [visualContext], wide-angle front view, establishing shot, same exact room, same furniture, same wall color, same floor, photorealistic",
    "MESMO AMBIENTE + ângulo 2: [mainSubject] in [visualContext], medium shot from left side, same exact room, same furniture placement, same decorations, photorealistic",
    "MESMO AMBIENTE + ângulo 3: [mainSubject] in [visualContext], close-up detail shot of textures, same exact room, same materials, same color palette, photorealistic",
    "MESMO AMBIENTE + ângulo 4: [mainSubject] in [visualContext], shot from right side, same exact room, same window, same curtains, same lighting, photorealistic",
    "MESMO AMBIENTE + ângulo 5: [mainSubject] in [visualContext], low angle from floor, same exact room, same ceiling, same chandelier, photorealistic",
    "MESMO AMBIENTE + ângulo 6: [mainSubject] in [visualContext], high angle overview, same exact room, same floor pattern, same rug, photorealistic"
  ]
}

# ⚠️🚨 REGRA ABSOLUTAMENTE CRÍTICA DOS GALLERY PROMPTS 🚨⚠️

VOCÊ DEVE gerar 6 prompts que mostram O MESMO CÔMODO visto de 6 ÂNGULOS DE CÂMERA diferentes!
NÃO invente cômodos diferentes! É o MESMO espaço, MESMA decoração, MESMA parede, MESMO piso!

❌ PROIBIDO (cômodos diferentes):
- Prompt 1: "living room with fireplace..."
- Prompt 2: "modern kitchen..."  ← ERRADO! É outro cômodo!
- Prompt 3: "cozy bedroom..."  ← ERRADO! É outro cômodo!

✅ CORRETO (mesmo cômodo, só muda o ângulo da câmera):
- Prompt 1: "rustic wooden dining table in farmhouse dining room, wide-angle front view, same exact room same furniture same walls"
- Prompt 2: "rustic wooden dining table in farmhouse dining room, left side angle, same exact room same chairs same decorations"
- Prompt 3: "rustic wooden dining table in farmhouse dining room, close-up of wood grain, same exact room same table same plates"
- Prompt 4: "rustic wooden dining table in farmhouse dining room, right side angle, same exact room same window same curtains"
- Prompt 5: "rustic wooden dining table in farmhouse dining room, low angle from floor, same exact room same legs same floor"
- Prompt 6: "rustic wooden dining table in farmhouse dining room, bird eye view, same exact room same layout same rug"

⚠️ TODOS os prompts DEVEM conter "same exact room" para garantir consistência!

# ⚠️🚨 REGRA DO EXCERPT (RESUMO) - MÁXIMA PRIORIDADE 🚨⚠️

O campo "excerpt" DEVE SER VARIADO e NUNCA repetir o mesmo início!

🎲 NÚMERO SORTEADO PARA EXCERPT: ${Math.floor(Math.random() * 25) + 1}

❌ PROIBIDO NO EXCERPT:
- "Outro dia percebi" - BANIDO PERMANENTEMENTE
- "Outro dia eu estava" - BANIDO PERMANENTEMENTE
- "Eu estava pensando" - BANIDO PERMANENTEMENTE
- "Neste artigo" - BANIDO
- "Vou te contar" - BANIDO
- "Descubra como" - BANIDO

✅ USE O FORMATO DO NÚMERO ${Math.floor(Math.random() * 25) + 1} ABAIXO:

1. "Eu percebi que com pequenos ajustes em ${topic} você pode mudar completamente a aparência da sua casa. Espero que goste!"
2. "Sabe aquela sensação boa de lar? Com essas dicas de ${topic} você vai sentir isso todos os dias!"
3. "Confesso que errei muito até aprender sobre ${topic}. Agora compartilho tudo com você!"
4. "Depois de muito estudo sobre ${topic}, reuni as melhores dicas que uso nos meus projetos."
5. "${topic} é um tema que me fascina. Preparei um conteúdo especial com muito carinho pra você."
6. "Separei dicas práticas sobre ${topic} que aprendi na faculdade e em reformas de família."
7. "Quer transformar seu espaço? ${topic} é a chave! Te conto tudo que aprendi."
8. "Com amor e dedicação, preparei um guia completo sobre ${topic} baseado em experiências reais."
9. "Se você ama decoração, vai adorar estas dicas sobre ${topic}. Foi feito com carinho!"
10. "Reuni tudo que sei sobre ${topic} neste artigo. Espero que te inspire!"
11. "${topic} mudou minha forma de ver os espaços. Te conto como aplicar isso na sua casa!"
12. "Trago experiências pessoais e dicas práticas sobre ${topic}. Espero que aproveite!"
13. "Depois de muita pesquisa, descobri segredos incríveis sobre ${topic}. Vem comigo!"
14. "Preparei dicas especiais de ${topic} que vão fazer diferença no seu dia a dia."
15. "Com base no que aprendi na PUC, compartilho dicas valiosas sobre ${topic}."
16. "${topic} é mais simples do que parece! Te mostro como transformar seu espaço."
17. "Aqui está tudo que você precisa saber sobre ${topic}, com dicas que realmente funcionam!"
18. "Juntei dicas incríveis sobre ${topic} para você aplicar hoje mesmo na sua casa."
19. "Minha paixão por ${topic} me levou a criar este guia completo. Espero que goste!"
20. "Se tem algo que aprendi sobre ${topic}, é que pequenos detalhes fazem grande diferença."
21. "Vem descobrir comigo os segredos de ${topic}! Preparei tudo com muito cuidado."
22. "Este artigo sobre ${topic} foi feito pensando em você que quer transformar sua casa."
23. "${topic} sempre me encantou. Agora divido com você o que aprendi ao longo dos anos."
24. "Com carinho, reuni as melhores práticas de ${topic} para inspirar seu próximo projeto."
25. "Fique de olho nestas dicas de ${topic}! Espero que te ajudem como me ajudaram."

- content DEVE OBRIGATORIAMENTE incluir "## Perguntas Frequentes" com 8-12 perguntas numeradas em negrito`;

    const userPrompt = `Crie um artigo PROFUNDO, EMOCIONAL e ENVOLVENTE sobre: "${topic}"

# CONTEXTO

- Blog homegardenmanual.com focado em casa e jardim
- Público: pessoas comuns que querem deixar suas casas mais bonitas e funcionais
- Tom: confissão íntima de um estudante de arquitetura apaixonado pelo que faz
- Objetivo: fazer o leitor se emocionar, se inspirar e agir

# O QUE O GOOGLE PRECISA SENTIR (E-E-A-T)

1. **EXPERIÊNCIA REAL**: Conte no mínimo 2 histórias pessoais detalhadas
   - Um erro que você cometeu (com números e consequências)
   - Uma descoberta que mudou sua visão

2. **EXPERTISE**: Mostre conhecimento técnico traduzido para linguagem simples
   - Mencione o que aprendeu na PUC Minas
   - Cite professores, aulas ou projetos (invente nomes)

3. **AUTORIDADE**: Integre links de autoridade NATURALMENTE no texto
   - Casa Vogue, ArchDaily e Leroy Merlin devem aparecer em CONTEXTO NARRATIVO
   - Nunca jogue links no final do parágrafo

4. **CONFIANÇA**: Seja vulnerável e honesto
   - Admita quando você não sabe algo
   - Compartilhe fracassos, não só sucessos

# LEMBRE-SE

- Você é Keven Costa Vieira, estudante de Arquitetura da PUC Minas, 7º período
- Escreva em 1ª pessoa, como quem está confessando para um amigo íntimo
- ZERO termos técnicos - traduza tudo para linguagem do dia a dia
- MÍNIMO 2.200 palavras (ideal 2.500-3.000) - O AdSense penaliza conteúdo curto
- Inclua a tabela comparativa com 7+ linhas
- Inclua valores ESPECÍFICOS em Reais (R$ 1.847, não "aproximadamente R$ 2.000")
- NÃO GERE CONCLUSÃO EMOCIONAL - será adicionada separadamente

# CAMPOS OBRIGATÓRIOS NO JSON

⚠️ CRÍTICO - O JSON deve incluir TODOS estes campos preenchidos:
1. mainSubject: descrição em INGLÊS do elemento principal (ex: "modern fireplace with glass panels")
2. visualContext: descrição em INGLÊS do ambiente completo (ex: "contemporary living room with natural lighting")
3. galleryPrompts: array com EXATAMENTE 6 prompts de foto em INGLÊS
   ⚠️⚠️⚠️ REGRA ABSOLUTAMENTE CRÍTICA: Todos os 6 prompts devem mostrar O MESMO CÔMODO/ESPAÇO!
   ⚠️ É uma "visita virtual" - mesmo ambiente visto de 6 ângulos de câmera diferentes!
   ⚠️ Varie APENAS o ÂNGULO: frontal, lateral esquerda, close-up, lateral direita, ângulo baixo, ângulo alto
   ⚠️ NUNCA mostre cômodos diferentes (ex: sala, cozinha, quarto) - TODOS os prompts são do MESMO local!
   ⚠️ Inclua "same room same decor" em cada prompt para garantir consistência visual!
4. content DEVE incluir "## Perguntas Frequentes" com 8-12 perguntas NUMERADAS: "1. **Pergunta?**", "2. **Pergunta?**"
   ❌ NÃO use ### (H3) para perguntas! Use APENAS números + negrito!
   ❌ NÃO insira "Ainda tem dúvidas?" entre as perguntas!

# IMPORTANTE - SEM CONCLUSÃO EMOCIONAL

⚠️ A conclusão emocional NÃO deve ser gerada neste artigo.
Ela será gerada separadamente por um sistema especializado.
Passe direto do conteúdo para o FAQ com um encerramento breve.

# REGRA DE OURO

Cada parágrafo deve ter pelo menos uma IMAGEM MENTAL vívida.
Você é um best-seller. Escreva como tal.`;

    const response = await fetch("https://api.openai.com/v1/chat/completions", {
      method: "POST",
      headers: {
        "Authorization": `Bearer ${OPENAI_API_KEY}`,
        "Content-Type": "application/json",
      },
      body: JSON.stringify({
        model: "gpt-4o",
        messages: [
          { role: "system", content: systemPrompt },
          { role: "user", content: userPrompt }
        ],
        temperature: 0.85,
        max_tokens: 16000,
      })
    });

    if (!response.ok) {
      const errorText = await response.text();
      console.error(`OpenAI API error: ${response.status} - ${errorText}`);
      
      if (response.status === 429) {
        return new Response(
          JSON.stringify({ success: false, error: "Rate limit exceeded. Please try again later." }),
          { status: 429, headers: { ...corsHeaders, 'Content-Type': 'application/json' } }
        );
      }
      
      if (response.status === 401) {
        return new Response(
          JSON.stringify({ success: false, error: "Invalid OpenAI API key." }),
          { status: 401, headers: { ...corsHeaders, 'Content-Type': 'application/json' } }
        );
      }
      
      throw new Error(`OpenAI API error: ${response.status}`);
    }

    const data = await response.json();
    const content = data.choices?.[0]?.message?.content;

    if (!content) {
      console.error("No content in response:", JSON.stringify(data));
      throw new Error("No content was generated");
    }

    console.log("Raw response content:", content.substring(0, 500));

    let articleData;
    try {
      let cleanContent = content.trim();
      if (cleanContent.startsWith('```json')) {
        cleanContent = cleanContent.slice(7);
      }
      if (cleanContent.startsWith('```')) {
        cleanContent = cleanContent.slice(3);
      }
      if (cleanContent.endsWith('```')) {
        cleanContent = cleanContent.slice(0, -3);
      }
      
      cleanContent = cleanContent.trim();
      
      try {
        articleData = JSON.parse(cleanContent);
      } catch {
        console.log("[JSONParse] Direct parse failed, attempting extraction...");
        
        const firstBrace = cleanContent.indexOf('{');
        const lastBrace = cleanContent.lastIndexOf('}');
        
        if (firstBrace !== -1 && lastBrace !== -1 && lastBrace > firstBrace) {
          const jsonSubstring = cleanContent.substring(firstBrace, lastBrace + 1);
          console.log("[JSONParse] Extracted JSON length:", jsonSubstring.length);
          
          try {
            articleData = JSON.parse(jsonSubstring);
            console.log("[JSONParse] Extraction successful!");
          } catch (innerError) {
            console.log("[JSONParse] Attempting to fix malformed JSON...");
            
            let fixedJson = jsonSubstring
              .replace(/\n(?=(?:[^"]*"[^"]*")*[^"]*$)/g, '\\n')
              .replace(/\r/g, '');
            
            try {
              articleData = JSON.parse(fixedJson);
              console.log("[JSONParse] Fixed JSON parsed successfully!");
            } catch {
              console.error("[JSONParse] All parsing attempts failed");
              throw innerError;
            }
          }
        } else {
          throw new Error("No JSON object found in response");
        }
      }
    } catch (parseError) {
      console.error("Failed to parse JSON:", parseError, "Content:", content);
      throw new Error("Failed to parse generated article");
    }

    // Validar contagem de palavras (mínimo aumentado para 2200)
    const contentWordCount = articleData.content ? articleData.content.split(/\s+/).length : 0;
    console.log(`[ContentValidation] Word count: ${contentWordCount}`);
    
    if (contentWordCount < 1800) {
      console.warn(`[ContentValidation] ⚠️ CRITICALLY LOW: ${contentWordCount} words (minimum 2200)`);
    } else if (contentWordCount < 2200) {
      console.warn(`[ContentValidation] ⚠️ BELOW MINIMUM: ${contentWordCount} words (target 2200-3000)`);
    } else if (contentWordCount >= 2200 && contentWordCount <= 3000) {
      console.log(`[ContentValidation] ✅ EXCELLENT: ${contentWordCount} words (ideal range)`);
    } else if (contentWordCount > 3000) {
      console.log(`[ContentValidation] ✅ EXCEPTIONAL: ${contentWordCount} words (above target)`);
    } else {
      console.log(`[ContentValidation] ✅ Content meets standards: ${contentWordCount} words`);
    }

    // ============================================
    // VALIDAÇÃO DE TÍTULO
    // ============================================
    const BANNED_TITLE_PATTERNS = [
      /^descubra\s+/i,
      /^transforme\s+/i,
      /^aprenda\s+/i,
      /^veja\s+/i,
      /^confira\s+/i,
      /^conheça\s+/i,
      /^explore\s+/i,
      /^entenda\s+/i,
      /guia\s+completo/i,
      /guia\s+definitivo/i,
      /dicas\s+essenciais/i,
      /dicas\s+imperdíveis/i,
      /tudo\s+sobre/i,
      /segredos?\s+(para|de|do|da)/i,
      /o\s+segredo/i,
    ];

    const BANNED_TITLE_PHRASES = [
      'guia completo',
      'guia definitivo',
      'dicas essenciais',
      'dicas imperdíveis',
      'tudo sobre',
      'o segredo',
      'transforme sua',
      'descubra como',
      'aprenda a',
    ];

    function validateAndSanitizeTitle(titleArg: string, topicArg: string): string {
      if (!titleArg || typeof titleArg !== 'string' || titleArg.trim().length < 10) {
        console.warn('[TitleValidation] Invalid title, generating fallback');
        return generateFallbackTitle(topicArg);
      }

      const cleanTitle = titleArg.trim();
      const lowerTitle = cleanTitle.toLowerCase();
      
      console.log(`[TitleValidation] Checking: "${cleanTitle}"`);
      
      for (const phrase of BANNED_TITLE_PHRASES) {
        if (lowerTitle.includes(phrase)) {
          console.warn(`[TitleValidation] BANNED PHRASE: "${phrase}"`);
          return generateFallbackTitle(topicArg);
        }
      }
      
      for (const pattern of BANNED_TITLE_PATTERNS) {
        if (pattern.test(cleanTitle)) {
          console.warn(`[TitleValidation] BANNED PATTERN: ${pattern.toString()}`);
          return generateFallbackTitle(topicArg);
        }
      }

      if (cleanTitle.length > 75) {
        console.warn(`[TitleValidation] Title too long: ${cleanTitle.length} chars`);
        return cleanTitle.substring(0, 72) + '...';
      }

      console.log('[TitleValidation] Title passed validation ✓');
      return cleanTitle;
    }

    function generateFallbackTitle(topicArg: string): string {
      const topicLower = topicArg.toLowerCase();
      
      const topicTitleMap: Record<string, string[]> = {
        'área gourmet': [
          'Área Gourmet: Como Planejei o Espaço dos Sonhos',
          'Área Gourmet: Ideias que Cabem no Orçamento',
          'Churrasqueira e Área Gourmet: O Que Faz Diferença',
        ],
        'area-gourmet': [
          'Área Gourmet: Ideias Lindas pra Sua Casa',
          'Área Gourmet em Casa: Inspirações Reais',
        ],
        'lareira': [
          'Lareira em Casa: Aconchego que Faz Diferença',
          'Lareiras: Como Escolher a Certa pra Você',
        ],
        'jardim': [
          'Jardim em Casa: Ideias Simples que Encantam',
          'Meu Jardim: O Que Aprendi Cuidando de Plantas',
        ],
        'cozinha': [
          'Cozinha dos Sonhos: O Que Realmente Importa',
          'Minha Cozinha: Erros que Cometi e Você Não Precisa',
        ],
        'sala': [
          'Sala de Estar: Como Deixar Aconchegante de Verdade',
          'Decoração de Sala: O Que Funciona na Prática',
        ],
        'quarto': [
          'Quarto Aconchegante: O Que Faz Toda Diferença',
          'Meu Quarto: Ideias que Testei e Aprovei',
        ],
        'banheiro': [
          'Banheiro dos Sonhos: Vale Cada Centavo',
          'Reforma de Banheiro: O Que Eu Faria Diferente',
        ],
        'piscina': [
          'Piscina em Casa: Sonho Mais Perto do Real',
          'Área de Piscina: O Que Considerar Antes',
        ],
      };
      
      for (const [key, titles] of Object.entries(topicTitleMap)) {
        if (topicLower.includes(key)) {
          const randomIndex = Math.floor(Math.random() * titles.length);
          console.log(`[TitleValidation] Using fallback for "${key}"`);
          return titles[randomIndex];
        }
      }
      
      const topicWord = topicArg.split(/[\s-]+/)[0];
      const capitalizedTopic = topicWord.charAt(0).toUpperCase() + topicWord.slice(1).toLowerCase();
      
      const genericTemplates = [
        `${capitalizedTopic}: O Que Aprendi na Prática`,
        `${capitalizedTopic}: Ideias que Funcionam de Verdade`,
        `${capitalizedTopic} em Casa: Minha Experiência`,
        `${capitalizedTopic}: Como Fazer Sem Gastar Fortuna`,
      ];
      
      const randomIndex = Math.floor(Math.random() * genericTemplates.length);
      return genericTemplates[randomIndex];
    }

    // ============================================
    // VALIDAÇÃO DE EXCERPT
    // ============================================
    const BANNED_EXCERPT_PATTERNS = [
      /^transforme\s+/i,
      /^descubra\s+/i,
      /^aprenda\s+/i,
      /^veja\s+como/i,
      /^conhe[çc]a/i,
      /dicas\s+(essenciais|imperdíveis)/i,
      /guia\s+completo/i,
    ];

    const BANNED_EXCERPT_PHRASES = [
      'transforme sua casa',
      'transforme seu espaço',
      'dicas essenciais',
      'dicas imperdíveis',
      'guia completo',
      'neste artigo',
      'descubra como',
      'aprenda a',
    ];

    function validateAndSanitizeExcerpt(excerpt: string, titleArg: string, topicArg: string): string {
      if (!excerpt || typeof excerpt !== 'string' || excerpt.trim().length < 30) {
        console.warn('[ExcerptValidation] Invalid excerpt, generating fallback');
        return generateFallbackExcerpt(titleArg, topicArg);
      }

      const cleanExcerpt = excerpt.trim();
      const lowerExcerpt = cleanExcerpt.toLowerCase();
      const wordCount = cleanExcerpt.split(/\s+/).length;
      
      console.log(`[ExcerptValidation] Checking: "${cleanExcerpt.substring(0, 80)}..."`);

      for (const phrase of BANNED_EXCERPT_PHRASES) {
        if (lowerExcerpt.includes(phrase)) {
          console.warn(`[ExcerptValidation] BANNED PHRASE: "${phrase}"`);
          return generateFallbackExcerpt(titleArg, topicArg);
        }
      }

      for (const pattern of BANNED_EXCERPT_PATTERNS) {
        if (pattern.test(cleanExcerpt)) {
          console.warn(`[ExcerptValidation] BANNED PATTERN`);
          return generateFallbackExcerpt(titleArg, topicArg);
        }
      }

      if (wordCount < 12 || wordCount > 45) {
        console.warn(`[ExcerptValidation] Word count out of range: ${wordCount}`);
        return generateFallbackExcerpt(titleArg, topicArg);
      }

      console.log('[ExcerptValidation] Excerpt passed validation ✓');
      return cleanExcerpt;
    }

    function generateFallbackExcerpt(titleArg: string, topicArg: string): string {
      const topicLower = topicArg.toLowerCase();
      
      // ⚠️ TEMPLATES VARIADOS - SEM "Outro dia percebi"
      const templates = [
        `Eu percebi que com pequenos ajustes em ${topicLower} você pode mudar completamente a aparência da sua casa. Espero que goste!`,
        `Sabe aquela sensação boa de lar? Com essas dicas de ${topicLower} você vai sentir isso todos os dias!`,
        `Confesso que errei muito até aprender sobre ${topicLower}. Agora compartilho tudo com você!`,
        `Depois de muito estudo sobre ${topicLower}, reuni as melhores dicas que uso nos meus projetos.`,
        `${topicLower.charAt(0).toUpperCase() + topicLower.slice(1)} é um tema que me fascina. Preparei um conteúdo especial com muito carinho pra você.`,
        `Separei dicas práticas sobre ${topicLower} que aprendi na faculdade e em reformas de família.`,
        `Quer transformar seu espaço? ${topicLower.charAt(0).toUpperCase() + topicLower.slice(1)} é a chave! Te conto tudo que aprendi.`,
        `Com amor e dedicação, preparei um guia completo sobre ${topicLower} baseado em experiências reais.`,
        `Se você ama decoração, vai adorar estas dicas sobre ${topicLower}. Foi feito com carinho!`,
        `Reuni tudo que sei sobre ${topicLower} neste artigo. Espero que te inspire!`,
        `${topicLower.charAt(0).toUpperCase() + topicLower.slice(1)} mudou minha forma de ver os espaços. Te conto como aplicar isso na sua casa!`,
        `Trago experiências pessoais e dicas práticas sobre ${topicLower}. Espero que aproveite!`,
        `Depois de muita pesquisa, descobri segredos incríveis sobre ${topicLower}. Vem comigo!`,
        `Preparei dicas especiais de ${topicLower} que vão fazer diferença no seu dia a dia.`,
        `Com base no que aprendi na PUC, compartilho dicas valiosas sobre ${topicLower}.`,
      ];
      
      const randomIndex = Math.floor(Math.random() * templates.length);
      return templates[randomIndex];
    }

    // ============================================
    // APLICAR VALIDAÇÕES
    // ============================================
    const validatedTitle = validateAndSanitizeTitle(articleData.title, topic);
    console.log(`[TitleValidation] Final: "${validatedTitle}"`);

    const validatedExcerpt = validateAndSanitizeExcerpt(
      articleData.excerpt,
      validatedTitle,
      topic
    );
    console.log(`[ExcerptValidation] Final: "${validatedExcerpt.substring(0, 80)}..."`);
    
    const categoryMatch = categories.find(c => 
      c.name.toLowerCase() === articleData.category?.toLowerCase()
    );
    const categorySlug = categoryMatch?.slug || 'decoracao';
    const categoryName = categoryMatch?.name || 'Decoração';

    const slug = generateSlug(validatedTitle);
    const readTime = calculateReadTime(articleData.content);

    const rawExternalLinks = articleData.externalLinks || [];
    const validExternalLinks = await validateExternalLinks(rawExternalLinks, 3);
    
    console.log(`[LinkValidation] Final links: ${validExternalLinks.length}`);

    const imageData = validateAndSanitizeImageData({
      mainSubject: articleData.mainSubject,
      visualContext: articleData.visualContext,
      galleryPrompts: articleData.galleryPrompts,
    });

    console.log('[ImageValidation] mainSubject:', imageData.mainSubject);
    console.log('[ImageValidation] visualContext:', imageData.visualContext);
    console.log('[ImageValidation] galleryPrompts:', imageData.galleryPrompts.length);

    // ============================================
    // VALIDAÇÃO E INJEÇÃO DE FAQ
    // ============================================
    let finalContent = articleData.content;
    
    // Verificar se o FAQ existe no conteúdo
    const hasFAQ = /##\s*(FAQ|Perguntas\s+Frequentes)/i.test(finalContent);
    const hasFAQItems = /\d+\.\s+\*\*[^*]+\?\*\*/m.test(finalContent);
    
    console.log(`[FAQValidation] Has FAQ header: ${hasFAQ}, Has FAQ items: ${hasFAQItems}`);
    
    if (!hasFAQ || !hasFAQItems) {
      console.log('[FAQValidation] ⚠️ FAQ missing or malformed - injecting default FAQ');
      
      // Gerar perguntas baseadas no tópico
      const topicLower = topic.toLowerCase();
      const defaultFAQ = `

## Perguntas Frequentes

1. **Qual é o orçamento ideal para ${topicLower}?**

Depende muito do tamanho do projeto e dos materiais escolhidos. Na minha experiência, é possível começar com investimentos a partir de R$ 500 para projetos menores. O importante é planejar bem e priorizar o que faz mais diferença no resultado final.

2. **Quanto tempo leva para fazer ${topicLower}?**

Projetos simples podem ser concluídos em um fim de semana. Já reformas mais complexas podem levar de 2 a 4 semanas. Sempre recomendo adicionar uma margem de 20% no cronograma para imprevistos.

3. **Preciso contratar um profissional para ${topicLower}?**

Para projetos básicos, você mesmo pode fazer com as orientações certas. Mas para instalações elétricas, hidráulicas ou estruturais, sempre contrate profissionais qualificados. A segurança vem em primeiro lugar!

4. **Quais materiais são mais recomendados?**

Depende do seu orçamento e do resultado desejado. Materiais de qualidade intermediária costumam oferecer o melhor custo-benefício. Evite os mais baratos que podem dar dor de cabeça depois.

5. **Como economizar sem perder qualidade?**

Compare preços em pelo menos 3 lojas diferentes, aproveite promoções sazonais e considere materiais alternativos que dão o mesmo efeito. Às vezes, uma pintura bem feita faz mais diferença que um revestimento caro.

6. **Vale a pena fazer ${topicLower} eu mesmo?**

Se você tem habilidades manuais e tempo disponível, pode economizar bastante fazendo você mesmo. Mas seja honesto sobre suas limitações - um trabalho mal feito pode sair mais caro no final.

7. **Quais são os erros mais comuns que devo evitar?**

Os principais erros são: não medir corretamente, economizar demais em materiais essenciais, pular etapas de preparação e não considerar a iluminação do ambiente. Planejamento é tudo!

8. **Como saber se o resultado vai ficar bom?**

Antes de começar, faça um projeto visual mesmo que simples. Use aplicativos de decoração ou recorte fotos de revistas. Ter uma referência visual ajuda muito a tomar decisões durante a execução.

`;

      // Encontrar o ponto de inserção - antes da assinatura ou no final
      const signaturePatterns = [
        /\n---\s*\n\*\*Keven/i,
        /\n\*\*Com carinho,/i,
        /\n---\s*$/,
      ];
      
      let insertionPoint = finalContent.length;
      for (const pattern of signaturePatterns) {
        const match = finalContent.match(pattern);
        if (match && match.index !== undefined) {
          insertionPoint = Math.min(insertionPoint, match.index);
        }
      }
      
      // Inserir FAQ
      finalContent = finalContent.substring(0, insertionPoint) + defaultFAQ + finalContent.substring(insertionPoint);
      console.log('[FAQValidation] ✅ FAQ injected successfully');
    } else {
      console.log('[FAQValidation] ✅ FAQ found in content');
    }

    const article = {
      title: validatedTitle,
      slug,
      excerpt: validatedExcerpt,
      category: categoryName,
      categorySlug,
      content: finalContent,
      tags: articleData.tags || [],
      keywords: articleData.keywords || '',
      readTime,
      externalLinks: validExternalLinks,
      mainSubject: imageData.mainSubject,
      visualContext: imageData.visualContext,
      galleryPrompts: imageData.galleryPrompts,
    };

    console.log(`✅ Article generated: ${article.title}`);
    console.log(`📊 Word count: ${contentWordCount} | Read time: ${readTime}`);
    console.log(`🖼️ Main subject: ${article.mainSubject}`);
    console.log(`🎨 Gallery prompts: ${article.galleryPrompts.length} items`);

    return new Response(
      JSON.stringify({ success: true, article }),
      { headers: { ...corsHeaders, 'Content-Type': 'application/json' } }
    );

  } catch (error) {
    console.error("Error generating article:", error);
    return new Response(
      JSON.stringify({ 
        success: false, 
        error: error instanceof Error ? error.message : 'Unknown error' 
      }),
      { 
        status: 500,
        headers: { ...corsHeaders, 'Content-Type': 'application/json' } 
      }
    );
  }
});
